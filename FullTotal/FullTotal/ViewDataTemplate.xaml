<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:n="clr-namespace:FullTotal.Navigation"
    xmlns:vm="clr-namespace:FullTotal.ViewModels"
    xmlns:v="clr-namespace:FullTotal.Views"
    xmlns:c="clr-namespace:FullTotal.Controls">

    <DataTemplate DataType="{x:Type n:NavigationManager}">
        <c:NavigationBanner />
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:AttractScreenViewModel}">
        <v:AttractScreenView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:HomeScreenViewModel}">
        <v:HomeScreenView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ArticleViewModel}">
        <v:ArticleView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:VideoViewModel}">
        <v:VideoView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:PannableContentViewModel}">
        <v:PannableContentView />
    </DataTemplate>

</ResourceDictionary>
