<Window x:Class="imageViewerALa.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Picture Viewer" Height="350" Width="700" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="350*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <Label Content="Git hub!" Height="28" Name="labelPath" Width="675"  />
        </StackPanel>
        <Rectangle Grid.Row="1" Fill="#FF293875" />
        <Image Grid.Row="1" Margin="15" Name="imagePicture" Stretch="Uniform" MouseLeftButtonDown="imagePicture_MouseLeftButtonDown"/>
    </Grid>
</Window>
